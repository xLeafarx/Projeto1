// Operadores matemáticos
/*
var valorum = 5;
var valordois = 4;
var valortres = 9;
var total;

//total = valorum + valordois;
//total = valorum + (valordois * valortres);
total = (valorum + valordois) * valortres;

//console.log(`O valor total é ${total}`);
console.log(total);
*/

// Potenciação (**)

var valorum = 10;
var valordois = 2;
var total;
 //total = valorum ** valordois;
 //console.log(total);


// resto (%) (retorno o resto de uma divisão)
total = 15 % 2
console.log(total);